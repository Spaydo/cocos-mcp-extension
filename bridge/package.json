{
  "name": "cocos-mcp-bridge",
  "version": "1.0.0",
  "description": "Standalone MCP bridge for Cocos Creator editors - auto-discovers and proxies to running editor instances",
  "main": "dist/index.js",
  "bin": {
    "cocos-mcp-bridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
